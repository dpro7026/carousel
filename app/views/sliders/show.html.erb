<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @slider.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @slider.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@slider.image.url(:large)) %>
</p>

<%= link_to 'Edit', edit_slider_path(@slider) %> |
<%= link_to 'Back', sliders_path %>
